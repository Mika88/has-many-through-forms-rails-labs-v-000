<%= form_for(@post) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.collection_select :categories, Category.all, :name, :name %>
  <%= f.submit %>
<% end %>
